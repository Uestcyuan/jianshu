import reducer from './reducer';
import * as actionCreator from './actionCreator';
import * as actionType from './actionType';

export { reducer, actionCreator, actionType };

//这个index.js是header目录的出口文件，引入这里的文件全通过index.js引入